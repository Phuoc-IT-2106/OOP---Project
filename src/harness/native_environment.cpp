#include "native_environment.h"

// NativeEnvironment methods are inline in environment.h. Keeping this
// translation unit in the harness target documents the module boundary while
// preserving compatibility with existing includes.
