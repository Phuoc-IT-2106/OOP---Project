#pragma once

#include "environment.h"

// NativeEnvironment is declared in environment.h because older project code
// already includes that header directly. This forwarding header keeps the
// required NativeEnvironment module path available without duplicating the type.
